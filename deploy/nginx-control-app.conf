# Nginx: проксирование на Node.js (control-app)
# После копирования: sudo nginx -t && sudo systemctl reload nginx

upstream control_app {
    server 127.0.0.1:3001;
    keepalive 32;
}

# Открытие по IP сервера (default_server)
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;

    client_max_body_size 12M;
    client_body_timeout 120s;

    location / {
        proxy_pass http://control_app;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_connect_timeout 60s;
        proxy_send_timeout 120s;
        proxy_read_timeout 120s;
    }
}

# Если есть домен — раскомментируйте и замените your-domain.ru
# server {
#     listen 80;
#     listen [::]:80;
#     server_name your-domain.ru www.your-domain.ru;
#
#     client_max_body_size 12M;
#
#     location / {
#         proxy_pass http://control_app;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_read_timeout 90s;
#     }
# }
